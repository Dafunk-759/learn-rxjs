{
  "name": "learn-rxjs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "tsc:w": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "prettier": "^2.5.1",
    "vite": "^2.8.0"
  }
}
